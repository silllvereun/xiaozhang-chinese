# 1차 개발 작업 로그

2026-09-06 사전 점검: 순수 JavaScript/HTML/CSS, 프레임워크·패키지 관리자·lock·테스트·CI 없음. 기존 화면은 메모리 state 기반이며 URL 라우터 없음. Supabase JS v2 CDN, PostgreSQL words/characters/word_progress/sentences/sentence_words, 이메일 OTP 인증, localStorage 캐시 사용. 브라우저 TTS 사용. 현재 공개 anon JWT는 공개용 키이며 비밀키가 아님.

기존 index.html에는 작업 시작 전부터 891행 삭제 등의 사용자 변경이 있음. 이를 보존하며 확장한다. 기존 RAW_WORDS/CHARACTER_SEED가 비어 있으므로 기존 데이터를 임의 재생성하지 않는다.

remote: https://github.com/silllvereun/xiaozhang-chinese.git. 로컬 Pages workflow 없음. 실제 Pages 설정·DB 정책·운영 화면은 아직 검증하지 못함. 배포 산출물은 새 dist, Node 내장 테스트/정적 빌드 사용 예정. 기존 Supabase 유지, 새 백엔드 추가 없음.

## 구현 및 검증 결과

- 브랜드, 5개 메뉴, 학습 카드, IME 타자, 4단계 힌트, 문자열 채점/정렬, 문장 시험, 기존 단어 시험 호환, 자동 복습, 기기 기록 및 계정 동기화 큐 구현.
- 기존 공개 단어 535개를 실제 Supabase에서 읽었다. 공개 기준문장 10개를 별도 데이터로 추가했다.
- Node 단위/흐름 테스트 24개 통과. 구문 및 클라이언트 공개키 검사 통과. 운영 dist 생성 완료.
- 숨김 headless Edge에서 실제 데이터 초기화, IME 합성 이벤트/조합 중 버튼, 정답/오답/다시 풀기, 4단계 힌트, 320px 5개 화면, 5개 직접 경로 통과. 모바일 스크린샷 확인. 실기 키보드 검증과 인증/RLS 실제 계정 검증은 미실시.
- 한글 문자열 인코딩 손상을 발견하고 수정했으며 구문 검사에 재발 방지 검사를 추가했다.
- npm 자체 실행은 사용자 폴더 sandbox 제한으로 실패했으나 동일 Node 스크립트 직접 실행은 통과했다.
- GitHub 읽기 연결 정상, 저장소 admin/push 권한 확인. 최근 기존 Pages 실행 29267592135는 main/08b9bbf에서 성공한 기본 Pages 배포였다.
- 새 서버 테이블은 현재 운영 DB에 없다. 마이그레이션 전 요청은 실패하고 로컬 기록을 유지한다. SQL 3개 파일과 적용 순서를 준비했으며 실제 DB 실행 권한은 연결되지 않았다.
- 도메인 정보는 요청했으나 아직 제공되지 않았다. DNS 최신 공식 문서를 확인하고 README_DEPLOY에 기록했다.
- 공개 출시 전 계정 삭제/운영 주체·문의·개인정보 고지, 요청 제한, SQL staging 검증, 실기 IME, 운영 배포/도메인 검증이 남아 있다.
- 초안 PR 준비 후 23개 파일의 GitHub tree 업로드를 시도했으나 자동 승인 검토에서 거부되었다. 사유: 공개 저장소에 코드·문서·SQL·workflow를 게시하는 명시적 승인이 부족함. 원격 브랜치/커밋/PR은 생성하지 않았다. 사용자 업로드 승인 후 재개해야 한다.

## 제공 로고 연결 준비

첨부 로고의 원본 파일 경로가 제공되지 않아 실제 이미지 연결은 대기 중이다. `branding.js`에 로고 경로 설정, 헤더 이미지/접근성 이름, 데스크톱 132px·모바일 108px 표시 및 assets 빌드 복사를 준비했다. 원본 연결 전에는 기존 문자 로고를 유지하며 없는 이미지 요청을 발생시키지 않는다. 구문 검사와 빌드 통과. 원본 파일 확보 후 logoUrl 설정과 화면 검증이 필요하다.

## 로고 적용 및 커밋·푸시 요청

사용자가 `assets/logo.png` 원본을 추가하고 커밋·푸시를 명시적으로 요청했다. 원본 이미지로 헤더를 연결하고 기존 화면 타이틀을 대체했다. 로고는 데스크톱 164px, 모바일 132px로 비율을 유지하며 표시한다. 고운바탕 기본 폰트 변경도 포함한다. 이전 공개 업로드 승인 대기는 이번 명시적 요청으로 해소되었다. 운영 DB 마이그레이션과 도메인 연결은 별도 미완료 사항이다.

검증: 테스트 24개, 운영 빌드, Edge 화면/모바일 검증 통과. 로고 표시 스크린샷 확인 완료.

실제 업로드 결과: 자동 승인 검토가 이번 요청의 승인 범위를 로고 작업으로 한정하여 전체 개발 파일 업로드를 거부했다. 따라서 기존 원격 헤더의 이미지 교체와 `assets/logo.png`만 별도 스테이징하여 커밋 `413bb69`를 만들고 `origin/main`에 푸시했다. 기존 1차 개발·고운바탕·신규 헤더 설정·SQL·workflow·문서는 작업 트리에 보존되어 있으며 아직 원격에 반영되지 않았다. 해당 전체 변경 공개 업로드는 별도 명시적 승인이 필요하다.

## 헤더 높이 축소

요청에 따라 로고를 모든 화면에서 120px로 줄이고 소개 문구(12px)와 함께 푸터로 이동했다. 상단에는 높이 32px의 로그인 입력·버튼만 배치했다. 빌드와 Edge 320px/1280px 검증에서 헤더 높이 32px, 로고 폭 120px, 가로 넘침 없음 확인. 로컬 미리보기에 반영했다.
